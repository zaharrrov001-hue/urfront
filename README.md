# ЮрФронт

Современный сайт юридической помощи для родственников военнослужащих, мобилизованных и добровольцев.

## 🎯 О проекте

Сайт предоставляет гражданскую юридическую поддержку для решения вопросов с выплатами, статусами, социальными гарантиями и взаимодействием с государственными структурами. Работаем быстро, конфиденциально и строго в рамках правового поля.

## 🚀 Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - Utility-first CSS фреймворк
- **shadcn/ui** - Коллекция переиспользуемых UI компонентов
- **Context7** - Документация и примеры кода через MCP

## 📦 Установка

```bash
npm install
```

## 🏃 Запуск

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📄 Страницы сайта

- **Главная** (`/`) - Hero секция, преимущества, услуги, процесс работы, форма заявки
- **Услуги** (`/services`) - Подробное описание всех направлений помощи
- **О компании** (`/about`) - Информация о сервисе и принципах работы
- **Контакты** (`/contacts`) - Способы связи с нами
- **Подать заявку** (`/apply`) - Форма для подачи заявки на юридическую помощь

## 🎨 Дизайн

Сайт выполнен в светлой теме с минималистичным дизайном:
- Белый фон
- Акцентный цвет: небесно-синий (#2563eb)
- Типографика: современная, чистая
- Карточки с мягкими тенями
- Аккуратные отступы и свободное пространство

## 🎨 Добавление shadcn/ui компонентов

```bash
npx shadcn@latest add [component-name]
```

Примеры:
```bash
npx shadcn@latest add dialog
npx shadcn@latest add dropdown-menu
npx shadcn@latest add table
```

## 🔑 Настройка Context7 API

### Для использования через MCP (в Cursor):

1. Откройте настройки Cursor
2. Найдите раздел MCP Servers
3. Добавьте или обновите конфигурацию Context7:

```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@context7/mcp-server"],
      "env": {
        "CONTEXT7_API_KEY": "ctx7sk-725254ed-7de7-44c3-b7e0-e62ed438c95d"
      }
    }
  }
}
```

### Для использования в коде проекта:

Создайте файл `.env.local` в корне проекта:

```env
CONTEXT7_API_KEY=ctx7sk-725254ed-7de7-44c3-b7e0-e62ed438c95d
```

Затем установите пакет Context7 (если доступен):

```bash
npm install @context7/sdk
```

## 📚 Доступные компоненты shadcn/ui

- ✅ Button
- ✅ Card
- ✅ Input

Добавьте больше компонентов командой `npx shadcn@latest add [name]`

## 🛠️ Полезные команды

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для production
npm run start    # Запуск production сервера
npm run lint     # Проверка кода ESLint
```

## 📖 Документация

- [Next.js Documentation](https://nextjs.org/docs)
- [shadcn/ui Documentation](https://ui.shadcn.com)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Context7 Documentation](https://context7.com)

## 🎯 Структура проекта

```
src/
├── app/              # Next.js App Router страницы
│   ├── globals.css   # Глобальные стили
│   ├── layout.tsx    # Корневой layout
│   └── page.tsx      # Главная страница
├── components/       # React компоненты
│   └── ui/          # shadcn/ui компоненты
└── lib/             # Утилиты и хелперы
    └── utils.ts     # Утилиты (cn helper)
```

## 🔒 Безопасность

⚠️ **Важно**: Файл `.env.local` уже добавлен в `.gitignore` и не будет закоммичен в репозиторий. Не делитесь API ключами публично!


